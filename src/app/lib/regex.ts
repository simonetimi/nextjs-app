export const specialCharRegex = /[^A-Za-z0-9]/; // Matches special characters
export const numberRegex = /\d/; // Matches numbers
export const lowercaseRegex = /[a-z]/; // Matches lowercase letters
export const uppercaseRegex = /[A-Z]/; // Matches uppercase letters
